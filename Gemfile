# =============================================================================
# Labhole — Gemfile
# =============================================================================
# Uses the github-pages gem to ensure local builds match GitHub Pages exactly.
# =============================================================================

source "https://rubygems.org"

# GitHub Pages meta-gem — pins Jekyll and all plugins to the versions
# used by the GitHub Pages build environment.
gem "github-pages", group: :jekyll_plugins
